[project]
name = "egregore"
version = "0.1.0"
description = "A collective dreaming engine for gathered spaces"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Benjamin Life" }]
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",
    "pyyaml>=6.0",
    "numpy>=1.26",
    "httpx>=0.27",
    "websockets>=12.0",
]

[project.optional-dependencies]
# Local transcription. parakeet is the recommended engine on Apple silicon
# (int8 ONNX, ~8x realtime); whisper is the CPU-anywhere alternative.
parakeet = ["onnx-asr>=0.2", "onnxruntime>=1.17"]
whisper = ["faster-whisper>=1.0"]
asr = ["faster-whisper>=1.0"]                     # kept for older instructions
# A real microphone. webrtcvad-wheels, not webrtcvad: the original imports
# pkg_resources, which modern setuptools no longer ships.
mic = ["sounddevice>=0.4", "webrtcvad-wheels>=2.0"]
# Everything a local party needs, in one flag.
local = ["onnx-asr>=0.2", "onnxruntime>=1.17", "sounddevice>=0.4", "webrtcvad-wheels>=2.0"]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
]

[project.scripts]
egregore = "egregore.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["egregore"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = ["E501"]
